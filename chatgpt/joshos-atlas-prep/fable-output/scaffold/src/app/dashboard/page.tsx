export default function Dashboard() {
  return (
    <div>
      <h1>JoshOS Atlas — Dashboard (scaffold)</h1>
      <ul>
        <li>Repo count: (seed import pending)</li>
        <li>Open actions: (seed import pending)</li>
        <li>P0 risks: (seed import pending)</li>
        <li>Broken deployments: (seed import pending)</li>
      </ul>
    </div>
  )
}
